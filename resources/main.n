add: int, int -> int
add :: a, b => a + b 

main :: {
	a:=1
	b:=2

	res :: {
	  x (a, b)
	}

	x := if x then y else n

	i := x() or return

	// python list comprehension
	positions: vec2[] = [x; for x in range 0..10; x* 10]
}

main :: {
	
	x := add 10 10 	    *10 30                     .

	xcolon_equalsaddopen_paren10comma10close_paren
}
